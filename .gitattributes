# Several long-lived instruction files are CRLF; treat the carriage return as
# part of the line ending while still checking ordinary trailing whitespace.
.github/instructions/*.md whitespace=trailing-space,space-before-tab,cr-at-eol
